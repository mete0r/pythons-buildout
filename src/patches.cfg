[pythons-patches]
recipe=hexagonit.recipe.download
url=${:url-base}/patches.tar.gz
destination=${buildout:parts-directory}/${:_buildout_section_name_}
strip-top-level-dir=true

url-base=${buildout:directory}
directory=${:destination}
